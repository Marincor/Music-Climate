import animationError from '../../../public/assets/lotties/404.json'

export const defaultOptions = {
    loop: true,
    autoplay: true,
    animationData: animationError,
    rendererSettings: {
      preserveAspectRatio: "xMidYMid slice"
    }
  };